Based on the example provided, add a module .lagda.md file to src/examples. 

After creating a example use agda to compile to ensure the example is correct.

Avoid the use of levels. Instead just use Set and Set₁ etc.

Ensure to to check the src/definitions folder. Always prefer a definition in this folder over a standard library construct.

Do not overbuild the example. Keep it as simple as possible while still adhering to the text.

New modules must be added to src/Everything.agda

Use the template scripts/example-template.lagda.md
